{
  "name": "goatmouth",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step (static site)'",
    "test:e2e": "playwright test",
    "test:e2e:report": "playwright show-report",
    "test:e2e:ci": "playwright test && node scripts/publish-playwright-report.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
